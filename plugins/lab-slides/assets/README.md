# assets — 共通画像ライブラリ(プラグイン層)

全プロジェクトで再利用する画像の原本置き場(研究室ロゴ・定番の概念図など)。

- ここに置いた画像は `/plugin update` で全マシンのキャッシュに配られる
- スライドで使うときは、ポータビリティのため各プロジェクトの
  `.lab-slides/figures/` にコピーして相対パスで参照する(promote.sh が補助)
- プロジェクト固有の実験グラフ等はここではなく各PJの `.lab-slides/figures/` へ
