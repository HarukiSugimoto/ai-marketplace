# 実装計画: <題名>

- 日付: <YYYY-MM-DD>
- slug: <slug>
- 設計: [design.md](./design.md)
- 状態: 承認済み / レビュー待ち

## 全体制約

<PJ 全体にかかる要件を、設計メモから逐語でコピーして1行ずつ。
 ライブラリのバージョン下限 / 命名規則 / 対応プラットフォーム / 文言のルール等。
 全タスクの要件に暗黙に含まれるものとして扱う。無ければ「無し」。>

-

## スコープ宣言

**この計画で触るのは以下のファイルだけ。** ここに無いファイルを変更する必要が出たら、
build-run は作業を止めてユーザーに確認する(黙って広げない)。

| ファイル | 新規/変更 | 何をするか |
|---|---|---|
| | | |

**触らないと明言するもの**(紛らわしいので先に宣言する):

-

## 受入条件 → テスト対応

設計の受入条件を、**どのテストが判定するか**まで落とす。
テストで判定できないものは、代わりに何を証拠にするかをここで決めておく
(検証段階で基準を後出しさせないため)。

| # | 受入条件 | 判定するテスト | テストが無い場合の証拠 |
|---|---|---|---|
| 1 | | `__tests__/xxx.test.ts` の `〜のとき〜になる` | — |
| 2 | | — | <コマンド出力 / 実際の操作手順と結果> |

## タスク

各タスクは **RED(失敗するテストを書く) → 失敗を目視 → GREEN(最小実装) → REFACTOR** の順で進む。
1タスク = 1つの意味のある振る舞い。大きすぎるタスクは分割する。

### T1: <タスク名>

- **RED**: `<テストファイル>` に `<テスト名>` を追加。`<何が起きるべきか>` を検証する
- **失敗の期待**: `<どう失敗すれば正しいか。「未定義エラー」なのか「値が違う」なのか>`
- **GREEN**: `<最小実装の方針>`
- **触るファイル**: `<スコープ宣言のどれか>`
- **使うもの**: `<先行タスクから受け取る関数・型の正確なシグネチャ。無ければ「無し」>`
- **出すもの**: `<後続タスクが依存する関数名・引数・戻り値の型。無ければ「無し」>`
- [ ] RED 書いた / [ ] 失敗を目視した / [ ] GREEN 通した / [ ] REFACTOR した

### T2: <タスク名>

- **RED**:
- **失敗の期待**:
- **GREEN**:
- **触るファイル**:
- [ ] RED 書いた / [ ] 失敗を目視した / [ ] GREEN 通した / [ ] REFACTOR した

## テスト免除(あれば)

`tdd_mode: pragmatic` のときのみ。**免除するタスクと理由を明記する。**
理由が「面倒だから」「時間がない」は却下。書けないなら免除しない。

| タスク | 免除理由 | 代わりの証拠 |
|---|---|---|
| | | |

## リスク

<この計画で失敗しそうなところ。無ければ「無し」。>

## 気づき

<計画を立てる過程で目に入った、今回のスコープ外の問題。実行はしない、報告だけ。
 無ければ「無し」と書く。空欄で出さない。>
