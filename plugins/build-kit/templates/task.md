# <NN> — <タスク名>

**Blocked by:** <このタスクより先に終わっている必要がある番号。無ければ「なし — すぐ着手可」>

<依存がある場合は、番号のあとに括弧で理由を1行。例: `01(sortKey 型が要る)`>

## 何ができるようになるか

<このタスクを閉じると、ユーザーから見て何ができるようになるか。1〜2行。
 「関数を追加する」ではなく「未入力で保存すると『必須です』が出る」と書く。
 これが書けないなら、それは1レイヤーだけの水平タスクになっている。>

## RED

- **テスト**: `<テストファイル>` に `<テスト名>` を追加
- **検証すること**: `<何が起きるべきか>`
- **失敗の期待**: `<どう失敗すれば正しいか。「未定義エラー」なのか「値が違う」なのか>`

## GREEN

<最小実装の方針。>

## 触るファイル

<plan.md のスコープ宣言にあるものだけ。>

-

## インターフェース

- **使うもの**: `<先行タスクから受け取る関数・型の正確なシグネチャ。無ければ「無し」>`
- **出すもの**: `<後続タスクが依存する関数名・引数・戻り値の型。無ければ「無し」>`

> **このファイルだけで実装できる状態にしておくこと。** build-run は1タスクずつ進むので、
> 実装中に見えるのはこのファイルと plan.md だけ。隣のタスクが何という名前で何を返すかを
> 知る手段がここしかない。

## 進捗

- [ ] RED 書いた
- [ ] 失敗を目視した
- [ ] GREEN 通した
- [ ] REFACTOR した

## 気づき

<このタスクの実装中に目に入った、スコープ外の問題。実行はしない、報告だけ。
 無ければ「無し」と書く。空欄で出さない。>
